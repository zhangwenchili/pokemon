Add RESTful json-based APIs based on functionalities from phase1, so that another frontend project can replicate the same functionalities in phase1 using the APIs
Use Devise gem for implementing the complete user authentication/authorization framework
The web application sends a notification email (using username as email address) when user catches a new pokemon instance
